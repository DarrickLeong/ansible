---
- name: AWS Playbook Lab
  hosts: localhost
  connection: local
  tasks:
    - name: Try to connect to AWS
      ansible.builtin.ping:
    - name: Gather AWS account facts
      amazon.aws.aws_facts:
